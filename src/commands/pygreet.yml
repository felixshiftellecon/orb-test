description: >
  This command echos "Hello World" using a python script.
# What will this command do?
# Descriptions should be short, simple, and clear.

steps:
  - run:
      name: Python Greeting
      command: <<include(scripts/hello.py)>>